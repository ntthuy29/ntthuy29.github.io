doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        h1 Home Page
        h1 #{users[0].name}
        each user in users
            p Name: #{user.name}, hihi